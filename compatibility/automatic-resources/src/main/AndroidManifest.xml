<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application android:name=".ProbeApplication" android:label="@string/shell_label"
        android:theme="@style/ShellTheme" android:allowBackup="false">
        <activity android:name=".ProbeActivity" android:exported="true">
            <intent-filter><action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" /></intent-filter>
        </activity>
        <provider android:name=".EarlyProvider" android:authorities="${applicationId}.early" android:exported="false" />
        <provider android:name=".WorkerProvider" android:authorities="${applicationId}.worker" android:exported="false" android:process=":worker" />
    </application>
</manifest>
